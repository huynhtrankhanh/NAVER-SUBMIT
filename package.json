{
  "name": "uniflow-production",
  "version": "1.0.0",
  "description": "UniFlow Production Server - serves both frontend and backend on single port",
  "scripts": {
    "build": "cd client && npm run build && cd ../server && npm run build",
    "start": "cd server && NODE_ENV=production npm start",
    "build-and-start": "npm run build && npm start",
    "setup": "./setup.sh",
    "production": "./production.sh"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}